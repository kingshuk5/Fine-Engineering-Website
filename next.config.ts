import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  distDir: 'build',
};
module.exports = {
  output: 'standalone', // or other configuration
};
export default nextConfig;
